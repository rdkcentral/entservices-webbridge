set(autostart ${PLUGIN_APPNOTIFICATIONS_AUTOSTART})

if(PLUGIN_APPNOTIFICATIONS_STARTUPORDER)
set (startuporder ${PLUGIN_APPNOTIFICATIONS_STARTUPORDER})
endif()

map()
   key(root)
   map()
       kv(mode ${PLUGIN_APPNOTIFICATIONS_MODE})
       kv(locator lib${PLUGIN_IMPLEMENTATION}.so)
   end()
end()
ans(configuration)

