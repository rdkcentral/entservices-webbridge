set (autostart false)
set (preconditions Platform)
set (callsign "org.rdk.DownloadManager")

if(PLUGIN_DOWNLOADMANAGER_STARTUPORDER)
set (startuporder ${PLUGIN_DOWNLOADMANAGER_STARTUPORDER})
endif()

map()
   key(root)
   map()
       kv(mode ${PLUGIN_DOWNLOADMANAGER_MODE})
       kv(locator lib${PLUGIN_IMPLEMENTATION}.so)
   end()
end()
ans(configuration)

