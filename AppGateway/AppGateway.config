set (autostart ${PLUGIN_APPGATEWAY_AUTOSTART})
set (preconditions Platform)
set (callsign "org.rdk.AppGateway")

if(PLUGIN_APPGATEWAY_STARTUPORDER)
set (startuporder ${PLUGIN_APPGATEWAY_STARTUPORDER})
endif()
